<%- include('../partials/header') %>


<table>
  <thead>
    <tr>
      <th>Item Name</th>
    </tr>
  </thead>
  <tbody>
    <% collection.item.forEach(i => { %>
      <tr>
        <td><a href="/items/<%= i._id %>"><%= i %></a></td>
      </tr>
    <% }) %>
  </tbody>
</table>

<%- include('../partials/footer') %>
